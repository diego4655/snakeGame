Para tener en cuenta el proyecto se encuentra en net8

para iniciar el proyecto debido a que funciona dockerizado se deben correr los siguientes comandos en la terminar

- dotnet restore
- dotnet publish -c Release -o  out

una vez realizdo solo se hace el docker build y se crea  el contenedor
